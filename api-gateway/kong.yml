_format_version: "3.0"
_transform: true

services:
  - name: auth-service
    url: http://host.docker.internal:9000
    routes:
      - name: auth-route
        paths:
          - /auth

plugins:
  - name: cors
